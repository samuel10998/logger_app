apiVersion: v1
kind: Service
metadata:
  name: logger-service
  namespace: exam-samuel
spec:
  type: NodePort
  selector:
    app: logger
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30082
